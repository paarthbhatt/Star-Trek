'use client';

import { motion } from 'framer-motion';

// Minimal header that can be expanded later if needed, but currently invisible/minimal per user request.
export function Header() {
  return null;
}
